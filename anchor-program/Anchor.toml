[features]
seeds = false
skip-lint = false

[programs.devnet]
solana_pay_merchant = "PLACEHOLDER_RUN_anchor_keys_list_after_build"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
