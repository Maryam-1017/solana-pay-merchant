# Tell Nixpacks to use Node 20 — required by @solana/web3.js (ESM dependency)
# NOTE: [providers] syntax was rejected by Railway's bot; [variables] is correct
[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = []

[start]
cmd = "node bin/www"
